### Code for reproducing experiments

Example Usage
`python -m run_experiment --experiment math_dataset_customv2 math_softprompt_generator math_explainability --save_directory ./datasets/<folder name>`

This will build the dataset, train the soft prompts, and generate verbalizations. In that order.
